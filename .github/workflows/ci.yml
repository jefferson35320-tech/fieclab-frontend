name: CI Frontend FiecLab`n`non:`n  push:`n    branches: [ "main", "master" ]`n  pull_request:`n    branches: [ "main", "master" ]`n`njobs:`n  build-react:`n    runs-on: ubuntu-latest`n    steps:`n      - uses: actions/checkout@v4`n      - uses: actions/setup-node@v4`n        with:`n          node-version: "20"`n      - name: Instalar dependencias`n        run: npm install`n      - name: Compilar o projeto`n        run: npm run build --if-present`n        env:`n          CI: false`n`n  docker-build:`n    runs-on: ubuntu-latest`n    needs: build-react`n    if: github.event_name == "push"`n    steps:`n      - uses: actions/checkout@v4`n      - uses: docker/login-action@v3`n        with:`n          username: `${{ secrets.DOCKER_USERNAME }}` `n          password: `${{ secrets.DOCKER_PASSWORD }}` `n      - uses: docker/setup-buildx-action@v3`n      - uses: docker/build-push-action@v5`n        with:`n          context: .`n          file: ./Dockerfile`n          push: true`n          tags: |`n            fieclab/fiecpa2026:latest`n            fieclab/fiecpa2026:`${{ github.sha }}` `n`n  deploy-k8s:`n    runs-on: self-hosted`n    needs: docker-build`n    if: github.event_name == "push"`n    steps:`n      - uses: actions/checkout@v4`n      - run: kubectl apply -f deployment.yaml`n      - run: kubectl rollout restart deployment/frontend-deployment`n      - run: kubectl rollout status deployment/frontend-deployment --timeout=180s
